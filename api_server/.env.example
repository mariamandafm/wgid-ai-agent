KEYCLOAK_URL=http://localhost:8080
# Opcional: usado para chamadas servidor-a-servidor (busca do JWKS).
# Em Docker Compose, sobrescrito para http://keycloak:8080.
# KEYCLOAK_INTERNAL_URL=http://localhost:8080
REALM=agents
API_CLIENT_ID=api-server
PORT=3001
AUDIT_LOG_FILE=./audit.log
